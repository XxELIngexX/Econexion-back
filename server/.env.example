# ==== Econexion Chat API (.env.example) ====
# Copia este archivo a ".env" y ajusta los valores.

# Puerto del API+WebSocket (por defecto 4000).
PORT=4000

# --- MongoDB ---
# Opción A: local (recomendada para desarrollo).
MONGO_URI=mongodb://127.0.0.1:27017/econexion_chat

# Opción B: Atlas (comenta la local y descomenta esta; reemplaza los placeholders).
# MONGO_URI=mongodb+srv://<usuario>:<pass>@<cluster>/<db>?retryWrites=true&w=majority&appName=<appName>

# Secreto para firmar JWT (cámbialo en producción; mínimo ~32 caracteres).
JWT_SECRET=change-me-in-prod-32chars-min

# Orígenes permitidos para CORS (separados por coma).
# Incluye Expo (19006), React web (3000) y cualquier otro host que uses.
CORS_ORIGIN=http://localhost:19006,http://localhost:3000,*

# (Opcional) Nivel de logs: tiny|dev|combined
# LOG_LEVEL=dev
# (Opcional) Entorno
# NODE_ENV=development
